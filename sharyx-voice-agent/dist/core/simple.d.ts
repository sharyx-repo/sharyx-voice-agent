import { VoiceAgent } from './voice-agent';
import { SimpleAgentConfig } from './types';
/**
 * Simplified factory to create a Voice Agent with minimal configuration.
 */
export declare function createAgent(config?: SimpleAgentConfig): VoiceAgent;
